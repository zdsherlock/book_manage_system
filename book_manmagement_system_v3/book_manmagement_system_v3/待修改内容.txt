待修改内容：
1、根目录数据库test3.DB的库存列，表头改名；
2、关闭当前窗口，弹出管理员界面 <zd001>; 完成
3、为什么这里没有实现取值 <zd002>； 完成，赋值的时候错了
4、删除命令的逻辑思考<zd003>； 完成
5、删除对应数据 <zd004>； 完成
6、在派生类的成员函数，调用基类<zd005>；
7、代码异常，测试跳出 <zd006>； 完成
8、listcontrol 的变量是不是要到基类中去<zd007> 完成，脑抽了；
9、删除数据库对应数据 <zd008>；
10、//为什么这里可以取值，事件触发的时候应该还没有数据<zd009>




















待达成目标：
1、sqlite3怎么在指定目录生成；
2、C++怎么访问指定目录的文件；